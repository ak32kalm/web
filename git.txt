git(설치파일)
git init(초기화)
git add * (버전 관리할 대상 등록)
git commit (로컬 저장소에 작업파일을 커밋)
원격 저장소 생성
git push(로컬저장소 == 원격저장소) 동기화.


파일상태.
unstage git add명령 > staged  commit명령 > sync push명령

pull(원격기준 -> 로컬반영)
add commit pull
